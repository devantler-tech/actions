name: 🧪 setup-go-toolchain

on:
  push:
    branches: ["main"]
  pull_request:

permissions: {}

jobs:
  test-setup-go-toolchain:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176 # v2.17.0
        with:
          egress-policy: audit

      - name: 📑 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: 🧪 Run setup-go-toolchain
        id: setup-go
        uses: ./setup-go-toolchain
        with:
          go-version: stable

      - name: ✅ Verify Go is installed
        shell: bash
        run: go version

      - name: ✅ Verify output
        shell: bash
        run: |
          if [[ -z "${{ steps.setup-go.outputs.go-version }}" ]]; then
            echo "::error::go-version output is empty"
            exit 1
          fi
          echo "Go version: ${{ steps.setup-go.outputs.go-version }}"

  test-private-modules:
    runs-on: ubuntu-latest
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@f808768d1510423e83855289c910610ca9b43176 # v2.17.0
        with:
          egress-policy: audit

      - name: 📑 Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: 🧪 Run setup-go-toolchain with token
        uses: ./setup-go-toolchain
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ Verify GOPRIVATE is set
        shell: bash
        run: |
          goprivate=$(go env GOPRIVATE)
          if [[ "$goprivate" != *"github.com/devantler-tech"* ]]; then
            echo "::error::GOPRIVATE not set correctly: $goprivate"
            exit 1
          fi
          echo "GOPRIVATE: $goprivate"

  status-check:
    if: ${{ !cancelled() }}
    needs: [test-setup-go-toolchain, test-private-modules]
    runs-on: ubuntu-latest
    steps:
      - name: Check status
        run: |
          results=("${{ needs.test-setup-go-toolchain.result }}" "${{ needs.test-private-modules.result }}")
          for result in "${results[@]}"; do
            if [[ "$result" == "failure" ]]; then
              exit 1
            fi
          done
