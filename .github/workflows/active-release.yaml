name: 🎉 Release

on:
  push:
    branches:
      - main

permissions: {}


jobs:
  release:
    uses: devantler-tech/reusable-workflows/.github/workflows/create-release.yaml@f974f62a8cb396130fa084d209ad1fc17e99b9fc # v5.6.1
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}
